# devbox
Start with a minimal install of Debian Trixie, installed without a Desktop Environment,
and with sign-in as root disabled. Then:

`sudo apt install git`

`git clone https://github.com/CollinMcKinney/devbox.git`

`sudo bash devbox/install.sh`